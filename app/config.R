db_file <- '../data/bridge.sqlite'
db_passphrase <- 'BridgeWorkshop'

app_title <- 'CUNY MSDS Bridge Tutorial'

tutorials <- list(
	"1 - Installing R" = list(
		url = "/tutorials/R-01-setup-r.Rmd",
		prerequisites = NA
	)
)
